package com.zote.policy.service.domain.enums;

/**
 * Indicates how the policy was sold (sales channel).
 * Used for analytics, channel-specific flows, and UX.
 */
public enum SaleSource {
    WEB,
    MOBILE,
    AGENT,
    API
}
